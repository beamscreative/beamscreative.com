name = "beamscreative-com"
compatibility_date = "2026-09-08"

# Workers Builds + `npx wrangler deploy` (not Pages).
[assets]
directory = "./dist"
not_found_handling = "404-page"

# Set these in the Cloudflare dashboard (build + runtime):
# SANITY_PROJECT_ID=o8sj69wc
# SANITY_DATASET=production
# SANITY_API_VERSION=2025-02-19
# SANITY_API_READ_TOKEN=
# SANITY_API_WRITE_TOKEN= (needed for /api/profile-lead once wired as a Worker route)
